<div class="container">
    <div class="inner">
        <div class="tb-heading col-row tb-row">
            <div class="col-25">
              <button data-sort="displayName" class="active sort sort-down">
                <h4 class="tb-col-inner">{{t 'app.displaytitle'}}</h4>
                <i class="fa"></i>
              </button>
            </div>
        </div>
        <ul class="usergroups clearfix grid-layout">
            {{!-- User groups get rendered in here --}}
        </ul>

        <div class="action-buttons">
            <button class="btn tertiary-hollow refresh-all">{{t 'app.refresh'}}</button>
        </div>
    </div>
</div>